运行环境 python2.7 (不支持python3)(安装时将python2添加进环境变量)

需要第三方模块requests(pip install requests)

需要第三方包bs4(pip install beautifulsoup(beautifulsoup4))

运行操作：
	打开cmd,
	cd 文件路径，
	python main.py
	后根据输出的提示操作，需要自己输入学号，密码，场次编号

特别注意，本版本为内测版，不提供选择测试项目，是项目全选。看清楚场次编号，选到当天的预约是不能退的